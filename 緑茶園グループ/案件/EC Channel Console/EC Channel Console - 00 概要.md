---
tags:
  - 緑茶園
  - 案件
  - システム棚卸
  - 開発中システム
client: 緑茶園グループ
親論点: テーマ1 - 受注チャネル統合とツール複数人化
フェーズ: 開発中
created: 2026-09-04
updated: 2026-09-07
aliases:
  - EC Channel Console
---

# EC Channel Console（開発中）

親: [[00 MOC|緑茶園グループ MOC]] ／ 親論点: [[テーマ1 - 受注チャネル統合とツール複数人化]]

> [!info] この案件ノートの位置づけ
> [[テーマ1 - 受注チャネル統合とツール複数人化]] が扱う「断絶チャネルをどう解消するか」という論点に対する、**自社開発側の具体案**。テーマ1には他に「[[店舗アップ♪（外部ベンダー製品）]] を導入」「easyECSごと載せ替え」という選択肢があり、3案の比較表はテーマ1側にある。
>
> **収録ノート**：[[EC Channel Console - 認証情報取得手順]]（先方担当者への配布用）

## これは何か

複数のECモール（楽天市場／Amazon／Yahoo!ショッピング／au PAYマーケット／Shopify／Temu／LINEギフト）の受注データを、それぞれの公式APIで取得・表示する社内向けツール。緑茶園グループ向けに開発中で、[[03 業務フローとツール状況]] のSTEP3「受注」で挙げた**チャネル断絶（🟥）の解消**を目的としている。

- リポジトリ：`multi-channel-order-fetcher`（本Vaultとは別リポジトリ）
- 技術スタック：Next.js 16（App Router）／React 19／TypeScript／Tailwind CSS v4／fast-xml-parser（Yahoo・au PAYがXML応答のため）

## 棚卸課題との対応

| チャネル | 棚卸表での状態（[[04 ツールリファレンス]]） | EC Channel Console 実装状況 | 使用API |
|---|---|---|---|
| 楽天市場 | 🟩自動（easyECS経由でAPI連携済み） | ✅ 実装済み | RMS WEB SERVICE `searchOrder` / `getOrder` |
| Amazon | 🟧手作業（easyECSでCSV取込） | ✅ 実装済み | SP-API Orders v0（LWAトークン認証） |
| Yahoo!ショッピング | 🟧手作業（easyECSでCSV取込） | ✅ 実装済み | 注文検索API `orderList` / 注文詳細API `orderInfo` |
| au PAYマーケット | 🟧手作業（easyECSでCSV取込） | ⚠️ 実装済みだがデプロイ環境では未疎通（IP制限） | Wow!manager API `searchTradeInfoListProc` |
| Shopify（自社EC） | 🟥断絶（一元管理に取込不可） | ✅ 実装済み | Admin GraphQL API `orders` |
| Temu | 🟥断絶（コピー＆ペースト対応） | ⛔ スタブ（画面・認証情報フォームのみ、取得は未実装） | ― |
| LINEギフト | 🟥断絶（コピー＆ペースト対応） | 🔶 認証情報の項目は確定、受注取得は仕様書待ち | 出店者向けAPI（OAuth2。仕様書が非公開） |

> [!tip] この表が意味すること
> 棚卸表で「🟥断絶」だった Shopify（自社EC）は、受注取得APIとしてはすでに実装済み。残る断絶は Temu・LINEギフトのみになった。
> ただし [[06 触らない領域]] の通り easyECS＋クロスモール自体は「残す」判断。このツールは**一元管理システムの載せ替えではなく、断絶チャネルを補う位置づけ**として説明するのが正確。[[09 初回すり合わせ論点]] の論点1（一元管理システムの将来性）と直結する。

## 受注取得の方式（クロスモールとの対比）

> [!important] 2026-09-07 追記：既製品のほうが堅い、とは限らないことが分かった
> クロスモール（I'LL社）の初期設定資料（Ver.付き・最終更新2025/03〜2026/08）を入手し、チャネルごとの受注取得方式を突き合わせた。**5チャネル中2つで、EC Channel Console のほうが直接的な経路を使っている。**

| チャネル | クロスモール | EC Channel Console |
|---|---|---|
| 楽天市場 | API | API |
| Amazon | **注文レポートの定期ダウンロード** | Orders v0 を直接呼ぶ |
| Yahoo!ショッピング | API | API |
| au PAYマーケット | API | API |
| [[Shopify]] | **注文確認メールのパース** | Admin GraphQL API を直接呼ぶ |

この差は運用制約として表面化する。クロスモールの資料に明記されている副作用は次の2つ。

- **Shopify**：注文確認メールを取り込む方式のため、クロスモール専用のメールアカウントを用意し、他のメーラーで受信しないようにする必要がある（先に受信されると取り込めない）。クロスモールは取込後にメールサーバから全メールを削除する。フリーメールも非推奨。
- **Amazon**：クロスモールが一定間隔で注文レポートをダウンロードするため、店舗側が「1日1度、自分で注文レポートをダウンロードする」といった**既存の運用が行えなくなる**。

> [!tip] この事実が動かす判断
> [[テーマ1 - 受注チャネル統合とツール複数人化]] の3案比較で暗黙の前提になっていた「既製品のほうが実装として堅い」は、**少なくとも受注取得については成り立たない**。ただし比較の射程は受注取得だけで、在庫送信・出品・画像アップ・配送便変換はクロスモールの領分（EC Channel Console のスコープ外）。比較の軸を「受注取得の堅さ」と「業務カバー範囲」に分けて論じる必要がある。

## au PAYマーケットのIP制限（デプロイ運用の穴）

> [!warning] 2026-09-07 判明：au PAYマーケットだけ、デプロイした状態では通らない
> au PAYマーケットのAPIは Wow!manager に登録した送信元グローバルIPからのリクエストしか受け付けない（5チャネルで唯一）。**Vercel等のサーバーレス環境は送信元IPが固定されないため、登録のしようがなく、本番URLからの取得は認証エラー `0002` になる。** 2026-09-07 に実機で確認済み。
> 「複数人利用の壁」は解消済みだが、**au PAYマーケットに限っては、デプロイ構成そのものが新しい壁になっている。**

対処は「送信元IPを固定する」しかない。

| 対処 | 費用 | 備考 |
|---|---|---|
| Vercel Static IPs | 月$100／プロジェクト | Pro以上のアドオン。もっとも素直だが、この規模には高い |
| 固定IPのVPSにプロキシを立て、au PAYへの通信だけ経由させる | 月数百円〜 | そのVPSのIPを登録する。クロスモールと同じ構造を最小構成で真似る |
| au PAYマーケットだけローカル実行 | 0円 | 複数人利用・自動実行には乗らない。暫定策 |

> [!info] 一元管理SaaSはどうしているか
> クロスモールは**14個の固定IP**（`121.83.254.*` の自社データセンター帯＋AWS東京の `13.112.7.206` など）を店舗に登録させている。ロジレスも4個を公開。**この種のAPIに繋ぐために各社が固定IPを確保している**というのが実態で、「クラウドだから動的IP」という話ではない。裏を返せば、月数百円のVPS1台で同じ構造は作れる。

## LINEギフトは実装できるか（2026-09-07 調査）

> [!success] 結論：**技術的には可能。ブロックしているのは仕様書の入手だけ**
> [[クロスモール（I'LL社）]] はLINEギフトを**APIで**連携している。認証はショップCMSの「アプリ > 新規登録」で発行する OAuth2 の認証情報で、**審査は無く即時発行**。楽天やYahoo!のような利用申請・リードタイムも、au PAYのようなIP制限も無い。5チャネルの中でもっとも取得が簡単な部類。

| 項目 | 内容 |
|---|---|
| 認証方式 | OAuth2 認可コードフロー（client_id / client_secret / refresh_token） |
| 発行元 | ショップCMS「アプリ > 新規登録」（アプリ名・Webアプリケーション・Redirect URI・API利用規約に同意） |
| 申請・審査 | **不要**。即時発行 |
| 有効期限 | アクセストークン60日。refresh_token から自動再取得 |
| 前提条件 | 登録するスタッフの権限が「受注: 閲覧可 以上」かつ「商品: 閲覧・変更可」 |
| IP制限 | **無し** |

> [!warning] ただし着手できない — 仕様書が一般公開されていない
> 「LINEギフト・LINEショッピング出店者向けAPI」の仕様書は公開されておらず、**エンドポイントのホスト名すら分からない**。一元管理各社（クロスモール／GoQSystem／ロジレス／コマースロボ）はいずれもAPI連携しているが、公開資料は「発行した認証情報を弊社システムに入力してください」までで、認可URL・トークンエンドポイント・受注APIのパス・レスポンス構造のいずれも出していない。
> 推測で書くと **au PAYマーケットと同じ「実データで照合するまで正しさが分からない」状態**になるため、実装は保留した。→ [[08 要確認事項]]

**実装時に効くチャネル固有の制約**（[[クロスモール（I'LL社）]] の資料より）

- **注文者情報（氏名・メールアドレス）が取得できない。** 取れるのはお届け先情報だけ。ギフトという商品性質上の仕様で、注文者へのメール送信もショップCMS側で行う必要がある。**受注一覧に「注文者名」を出す前提の画面設計はそのままでは成立しない。**
- ポイント・売上金による値引きが金額に反映されない（差し引かれていない額が返る）。
- 受注確定前の「仮注文」がある（お届け先が住所を確定するまで）。取り込み対象を「発送待ち」以降に絞るか、仮注文も扱うかは要件次第。

リポジトリ側の現状：認証情報の項目（`LINEGIFT_CLIENT_ID` / `LINEGIFT_CLIENT_SECRET` / `LINEGIFT_REFRESH_TOKEN`）は確定済みで、マスタ設定から保存できる。`lib/channels/linegift.ts` の冒頭コメントに上記の前提を書き残してある。


## 複数人利用の壁（大半は解消済み）

→ 検討の全体像は [[テーマ1 - 受注チャネル統合とツール複数人化]]

> [!important] 2026-09-05 にリポジトリを直接確認し、旧記述が古いことが判明した
> 出典：`~/git/multi-channel-order-fetcher`（commit `d37daea` 時点。`4f29f79 docs: READMEをSupabase保存・ログイン構成に合わせて更新` で構成変更済み）。**このノートおよび [[テーマ1 - 受注チャネル統合とツール複数人化]] に書かれていた「複数人利用の壁」3点は、いずれもすでに解消されている。**

| 旧記述（〜2026-09-04） | 現在（コードで確認） |
| --- | --- |
| ~~`npm run dev` 専用。Vercel等にデプロイ不可~~ | デプロイ前提の構成。リポジトリREADMEに「デプロイ先（Vercelなど）の環境変数にも同じ値を設定」と記載 |
| ~~認証情報は `.env.local` へ直書き（`fs.writeFile`）。平文・同一マシン前提~~ | Supabaseの `multi_channel_order_fetcher` テーブルに保存。`service_role` 経由のサーバー専用処理のみで、保存値はAPI・フォームから読み戻さない |
| ~~ユーザーという概念自体が無い~~ | Supabase Auth によるログイン必須。`proxy.ts`（Next.js 16 の middleware 規約）が `/login` と静的アセット以外の全パス（画面・API両方）を保護。自己サインアップ不可 |

> [!warning] ただし「操作履歴」はまだ空いている
> [[12 現行の受注フロー（先方提供資料）]] で社長が挙げた要望のうち、**「スタッフごとのログイン」は満たされたが、「ステータスの追跡・メール送信者と日時の確認」＝ユーザー単位の操作履歴は未実装**。`multi_channel_order_fetcher_logs` は受注取得・設定保存の**エラーログ**であって監査証跡ではない。
> なお [[店舗アップ♪（外部ベンダー製品）]] 側も要件書16項目に操作履歴の記載が無く、**この点は依然として3案共通の穴**の可能性がある（要ベンダー確認）。

位置づけ：「第一次視認性確保策」であり、フル業務代替ではない。外部ベンダー製品 [[店舗アップ♪（外部ベンダー製品）]] とあわせて比較検討する。

## 使い方（概要）

1. `/settings`（マスタ設定）で、使いたいチャネルの認証情報を入力して保存（Supabaseの `multi_channel_order_fetcher` テーブルに保存）。
2. `/channels/<チャネル>` で「受注データを取得」を押すと、直近の受注が一覧表示される（取得期間は1/3/7/14/30日、既定7日）。
3. `/`（ダッシュボード）で全チャネルの接続設定状態を一覧できる。

## 認証・アクセス制御

- アプリ全体がログイン必須（Supabase Auth、メール＋パスワード）。自己サインアップ不可で、事前作成したアカウントのみ使える。
- 認証情報は `.env.local` への直書きではなく、Supabaseの **`multi_channel_order_fetcher`** テーブル（key/value）に保存する方式（サーバーレス環境の読み取り専用ファイルシステムでも動くようにするため）。
- 読み書きは `service_role` キー経由のサーバー専用処理のみ。クライアントに返すのは「設定済み／未設定」の真偽値だけで、保存済みの値自体はAPIからもフォームからも読み戻さない。
- 書き込めるキーは許可リストに限定。空欄のまま保存した項目は既存値を上書きしない。
- 受注取得・設定保存のエラーは `multi_channel_order_fetcher_logs` テーブルにも記録され、後から失敗内容を追える。

## 未検証の部分（実データでの照合が必要）

実装済み5チャネルは公開API仕様に沿って実装しているが、**実アカウントでのレスポンス照合はまだ**（ダミー認証情報での疎通・エラー処理確認まで実施済み）。

- **楽天**：`getOrder` から商品明細・注文者名・金額を取り出す部分、`orderProgress` コードのマッピング。
- **Amazon**：`BuyerInfo.BuyerName` はPII制限で返らないことがある（配送先氏名→`(非公開)` の順にフォールバック）。
- **Yahoo!ショッピング**：`PayStatus`／`ShipStatus` のコード対応表は公開情報からの推定を含む。
- **au PAYマーケット**：**API仕様が一般公開されておらず**、利用申請時に配布される。実装は公開情報からの推定を含み、**5チャネルの中で最も確度が低い**前提で確認が必要。
- **Shopify**：確度は高めだが、`customer` は保護対象顧客データの承認状況によって `null` になる場合がある。

## レート制限

- Amazon `getOrderItems`：0.5 req/sec（1件ずつ約2.1秒間隔、明細取得は先頭20件まで）
- Yahoo!ショッピング：1 query/sec（1件ずつ約1.1秒間隔、先頭20件まで）
- Shopify：GraphQLのコストベーススロットリング（`THROTTLED` をレート制限に変換）

## モックAPIでの検証

APIキーが手元にない段階でも、実APIの応答を模したモックサーバーに対して通しで動作確認できる（`npm run test:mock` で101項目を自動検証）。ただし**実APIが本当にこの形のレスポンスを返すかは検証できない**——確認できるのはアプリ側のロジックとリクエストの組み立てのみ。

## 次のアクション

- [ ] 実アカウントでの5チャネルのレスポンス照合（特にau PAYマーケット）
- [ ] **LINEギフト出店者向けAPIの仕様書を入手する**（入手できれば実装は可能。認証情報の項目は確定済み）→ 上記「LINEギフトは実装できるか」
- [ ] Temuの実装（クロスモールの連携資料も見つかっておらず、対応状況自体が不明）
- [ ] easyECS／クロスモールとの役割分担の最終決定 → [[09 初回すり合わせ論点]]
- [ ] **au PAYマーケットのIP制限をどう解くか決める**（Vercel Static IPs 月$100／固定IPのVPSにプロキシ／auだけローカル実行）→ 上記「au PAYマーケットのIP制限」
- [ ] au PAYマーケットの会員番号（`AU_MEMBER_ID`）を Wow!manager「店舗構築 > 店舗情報 > 登録情報」で目視確認する（APIからは判定不能）
- [ ] 複数人利用化する場合の技術移行（Postgres化・Vercelデプロイ・認証情報の暗号化保存）を [[店舗アップ♪（外部ベンダー製品）]] 導入との総コスト比較のうえで判断

## 関連ノート

- [[EC Channel Console - 認証情報取得手順]] — クライアント担当者への配布用ドキュメント
- [[クロスモール（I'LL社）]] — 受注取得方式の対比・au PAYの固定IP・LINEギフト連携仕様の一次情報
- [[03 業務フローとツール状況]] / [[04 ツールリファレンス]] / [[06 触らない領域]]
- [[テーマ1 - 受注チャネル統合とツール複数人化]] / [[店舗アップ♪（外部ベンダー製品）]] / [[Shopify]]
