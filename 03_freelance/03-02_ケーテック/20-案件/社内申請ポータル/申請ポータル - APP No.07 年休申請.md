---
tags:
  - ケーテック
  - 案件
  - APP
client: ケーテック株式会社
親論点: テーマ1 - 電子化をどこまで広げるか
フェーズ: 開発中
課題No: 7
created: 2026-09-05
updated: 2026-09-22
---

# APP No.07 年休申請

親: [[申請ポータル - 00 概要]] ／ [[00 MOC|ケーテック株式会社 MOC]]

| 項目 | 値 |
|---|---|
| リスト内部名 | `LeaveRequests` |
| 表示名 | 年休申請 |
| 定義 | `template/20_requests/07_leave-request.xml` |
| 要望部署 | 経営SPG |
| 元重要度 | —（「済」だが**やり直し**） |
| 承認段数 | 1段 |
| 状態 | ✅ リスト・フォーム構築済み ／ ❌ フロー未実装 |

## これは何か

**全APPの雛形**。最初に完成させ、共通部品（コンテンツタイプ・SPFxの申請エンジン・ステータス設計）を
確立したうえで他APPへ横展開した。

## なぜ「やり直し」なのか（このプロジェクト全体を規定した回答）

> [!important] 2026-08-17 の回答
> **「現状M365前任者しかメンテできない（PowerAppsが複雑なため）」**
>
> 機能の不具合ではなく**引き継げないこと**が理由。これが以降のすべての設計判断の背骨になった。
> → [[テーマ5 - 既存システムとの併存と属人化解消]] ／ [[旧PowerApps年休申請（前任者作）]]

さらに、現状は**3系統が併存している**と判明している。

1. 紙申請
2. 購買前任者が作ったシステム
3. M365前任者が作った PowerApps

**現物へのアクセス権は未付与**のため、既存データの移行要否は判断できていない。→ [[05 要確認事項]]

## 列（固有列のみ。共通列は [[申請ポータル - 共通列とコンテンツタイプ]]）

| 内部名 | 表示名 | 型 | 備考 |
|---|---|---|---|
| `Title` | 件名 | Text（必須） | **自動生成・常時グレーアウト**。`申請日(yyyymmdd)_休暇区分_氏名`（代理申請時は対象者の氏名） |
| `InitiatorType` | 起票区分 | Choice | 本人申請 / 代理申請（**2026-09-22追加**。慶弔連絡と同じ考え方だが「人事起票」相当は無い） |
| `TargetEmployee` | 対象者(代理申請) | Person | **2026-09-22追加**。代理申請のときだけ入力可。`ShowInEditForm="false"` |
| `LeaveType` | 休暇区分 | Choice | 年休 / AM休 / PM休 / 特別休暇 / 欠勤 / 遅刻 / 早退（**7区分**） |
| `StartDate` | 取得日(開始) | DateTime(DateOnly) | |
| `EndDate` | 取得日(終了) | DateTime(DateOnly) | AM休/PM休のときは開始日に固定・選択不可。**2026-09-22追加**: 遅刻/早退のときも同様に編集不可 |
| `LeaveDays` | 取得日数 | Number | 土日＋`BusinessCalendar`の会社休日を除外して自動計算。半休は0.5換算。遅刻/早退は空欄のまま**編集不可**（2026-09-22、以前は単に計算しないだけで手入力可能だった） |
| `Reason` | 事由 | Choice | 休暇区分＝特別休暇のときだけ「慶弔休暇 / 生理休暇 / 罹災休暇 / 裁判員休暇」の4件（`choicesFor`）。それ以外は従来の7件（体調不良 / 通院 / 親族(病気,怪我等) / 冠婚葬祭 / 学校・町内行事 / 免許取得・資格取得・受験 / その他）。**2026-09-22追加**。SharePoint列自体は両方の選択肢セットを合わせ持つ1つのChoice列 |
| `SpecialLeaveDetail` | 内容 | Choice | **2026-09-22追加**。`Reason=慶弔休暇`のときだけ表示。就業規則46条/16条の①〜⑦（例: `①本人が結婚するとき(5日)`） |
| `StartTime` | 時刻(開始) | Text | 遅刻/早退のときだけ表示。**2026-09-22**: 手入力からプルダウン選択式に変更（30分刻み・6:00〜22:00。列自体はText型のまま） |
| `EndTime` | 時刻(終了) | Text | 同上 |

### 休暇区分の変遷

| 時点 | 区分 |
|---|---|
| 2026-08-17 回答 | 年休 / AM休 / PM休 / 特別休暇 / 欠勤 / 遅刻 / 早退 / **外出**（8種） |
| 2026-08-27 依頼で削除 | 「外出」は利用実績が無いため削除 → **7区分** |

> [!info] 「出張」「直行直帰」を含めていない理由
> 2026-08-17 に先方は No.11（許可申請）を「No.7 へ統合」と回答したが、**社内判断で独立案件として
> 維持した**ため。この判断が先方に伝わっているかは未確認。
> → [[申請ポータル - APP No.11 許可申請（仮組み）]] ／ [[05 要確認事項]] V-3

## 依頼と対応の履歴

| 日付 | 先方の依頼 | 判断 | こちらの対応 |
|---|---|---|---|
| 2026-08-17 | やり直しの理由は「前任者しかメンテできない」 | ✅ | 全設計の最重要制約として採用 |
| 2026-08-17 | 勤怠は Prevision。連携システムは無い | ✅ | 連携をスコープ外に確定。「Prevision で選択 → Excel 書き出し → 社労士へメール」の現行運用は変えない |
| 2026-08-17 | 全社展開前提 | ✅ | 部署選択肢を全社分で仮置き |
| 2026-08-17 | 休暇区分8種・事由6種 | ✅ | `LeaveType` を8区分へ拡張、`Reason` を選択肢化 |
| 2026-08-27 | 開始/終了から取得日数を自動計算（土日除外） | ✅ | 暦日数から土日除外へ変更 |
| 2026-08-27 | AM休/PM休のとき終了日を選択不可に | ✅ | 開始日に固定してグレーアウト、0.5日換算 |
| 2026-08-27 | 早退・遅刻のとき時間入力を可能に | ✅ | `StartTime` / `EndTime` を条件表示で追加 |
| 2026-08-27 | 「外出」を削除 | ✅ | 選択肢から削除 |
| 2026-08-27 | 件名は「休暇区分＋氏名」を自動反映 | 🔁 | 件名の重複を避けるため申請日を頭に付けた |
| 2026-08-27 | 毎月20日締めのExcel/CSV出力 | 🗣 | **開発せず Excel の Power Query で対応**する手順を提示 |
| 2026-08-27 | 承認者を上長情報から自動設定（上長マスタ案） | ❌ | **2026-09-09、不採用で決着。** 代わりに「前回選んだ承認者を初期値表示」を実装 → [[テーマ2 - 承認者をどう決めるか]] |
| 2026-08-27 | 申請状態は申請者と承認者のみ確認できるように | ⚠ | 現状は承認者もリスト上は見えない旨を説明。「これで十分」と回答を得た |
| 2026-08-27 | 年休カレンダーの公開可否を選択制に | ⏸ | **要件のイメージが掴めず実装できないと判断**。確認予定 |
| 2026-08-27 | 特別休暇はシステム上「年休」と表示 | ⏸ | 〔対応状況を未確認〕 |
| 2026-08-27 | 取得理由はカレンダー上に表示しない | ⏸ | カレンダー自体が未実装のため保留 |
| 2026-09-09 | 上司はマスタではなく履歴系にする（一度申請したら次はデフォルトで引っ張る） | ✅ | 自分がそのリストに直近提出した申請の`Approver`を初期値表示。リストごとに独立、上書き可 |
| 2026-09-09 | 営業日カレンダーを取得日数計算に反映する（土日＋カレンダー） | ✅ | 眠っていた`BusinessCalendar`を転用。受領した「2026年度カレンダー」から実データを作成(詳細は[[2026-09-09 承認者履歴・営業日カレンダー・従業員マスタ導入]]) |
| 2026-09-22 | カレンダー9/14の色分けは誤り、8/20も要確認。会社休日は土日＋四角枠のみ、祝日は含まず、△も除外 | ✅ | 先方から原本PDF（`2026年度カレンダー (2).pdf`）を受領し画像化して直接確認。9/14・8/20ともseed(`business-calendar.csv`)から削除。突き合わせで4/29・5/1・5/4・5/5・8/4・8/5・8/11・1/1の食い違いも発見・修正 |
| 2026-09-22 | 8/7〜8/17で取得日数7日と表示、正しくは4日 | ✅ | 「7日」自体はテナント側`BusinessCalendar`にお盆データ未投入で土日除外のみのフォールバックが効いた可能性が高い（要検証）。あわせて`HolidayType`を見ずBusinessCalendarの全行を除外していたコード側の欠陥も発見・修正し、`HolidayType="会社休日"`のみ除外するよう修正 |
| 2026-09-22 | 特別休暇に事由（休暇区分→事由：慶弔休暇→内容①〜⑦）のカスケード選択を追加したい | ✅ | `Reason`列を休暇区分に応じて選択肢が変わる方式(`choicesFor`)に拡張。新規列`SpecialLeaveDetail`(内容)を追加。就業規則46条/16条ベースで、慶弔連絡（APP No.02、慶弔見舞金規程ベース）とは別物 → [[2026-09-22 年休・慶弔・昼食の追加要望]] |
| 2026-09-22 | 遅刻/早退のとき終了日・日数を入力不可に、時刻入力を選択式に | ✅ | 終了日は開始日に固定、日数は空欄のままどちらも編集不可に(`computeLeaveFields`)。時刻(開始/終了)は30分刻み・6:00〜22:00のプルダウン選択に変更(`TIME_CHOICES`。列自体はText型のまま) |
| 2026-09-22 | 承認者Person Pickerで氏名検索だと表示されない人がいる | 🔁 | **部分対応。** メール検索が常に通るのは検索自体でなく`onValidateInput`のフリーテキスト経路(送信時に`sp.web.ensureUser`で解決)であり、氏名検索の結果件数上限(`MaximumEntitySuggestions`)が原因で該当者が埋もれている可能性が高いと判明。15→30に緩和したが、根本原因(該当者のEntra ID属性等)はテナント側の実機調査が必要 |
| 2026-09-22 | 休暇申請に代理申請機能がほしい | ✅ | 慶弔連絡と同じ考え方で`InitiatorType`(本人申請/代理申請)・`TargetEmployee`を追加。代理申請時は対象者の氏名を件名に使う |

## 毎月20日締めの出力（Power Query 案）

Power Automate や SPFx の追加開発は不要で、**総務側の Excel ブックだけで完結する**想定。

1. Excel の「データ」→「データの取得」→「SharePointリストから」で `LeaveRequests` へ接続
2. Power Query エディタで対象期間（前月21日〜当月20日）にフィルタ
3. 必要な列だけ読み込み、ピボットテーブル等で集計
4. 「更新」を押すたびに最新データが反映される

> [!info] 前提条件（2026-09-06 に整理し直した）
> Power Query は**開いている本人の権限で読む**ため、総務が閲覧・投稿レベルだと自分の分しか取れない。
> ただし `ReadSecurity=2` は **`Manage Lists`（Design 以上）権限を持つ人には適用されない**ため、
> **総務がサイト所有者ならそのまま全件取れる。まず現物を確認すること。**
>
> 取れない場合も、**Power Automate で月次に Excel/CSV を書き出せば権限を触らずに実現できる**
> （フローは接続アカウントの権限で読むため項目レベル制限を受けない）。
> → [[テーマ3 - 総務の全件閲覧をどう成立させるか]]
>
> また、フィルタに使う日付列を「取得日(開始)」にするか「申請日」にするかで意味が変わる
> （休暇を取った日基準か、申請した日基準か）— **要確認**。

## 未解決

- [x] ~~取得日数の計算に祝日・会社休日を含めるか~~ → **2026-09-09、`BusinessCalendar`を接続して対応**
- [x] ~~営業日カレンダーの赤字08/20・09/14が国民の祝日と一致しない~~ → **解消（2026-09-22）**。
  先方から原本PDF（`2026年度カレンダー (2).pdf`）を受領し、`pdftoppm`で画像化して1件ずつ直接確認した。
  9/14・8/20はいずれも原本に一切マークが無い平日で、`config/masters/business-calendar.csv`から削除した。
  **あわせて原本との突き合わせで9件の食い違いを追加発見**（4/29・5/1・5/4・5/5・8/4・8/5・8/11・1/1）。
  詳細は[[2026-09-22 年休・慶弔・昼食の追加要望]]参照
- [x] ~~推奨(三角・部署裁量)の日を祝日・会社休日と同列に取得日数計算から除外している~~ →
  **解消（2026-09-22）**。下記バグ修正と合わせて、除外対象を`HolidayType="会社休日"`のみに限定した
- [x] ~~★取得日数計算のバグ（8/7〜8/17で7日と表示、正しくは4日）~~ → **解消（2026-09-22）**。
  「7日」という結果自体は、テナント側`BusinessCalendar`にお盆期間のデータが未投入で土日除外のみの
  フォールバックが効いた結果である可能性が高い（実機での検証はしていない）。あわせて
  `RequestForm.tsx`が`HolidayType`を取得せず`BusinessCalendar`の全行（祝日・会社休日・推奨）を
  無差別に除外対象にしていた**コード側の欠陥**（データ投入後に別の不具合として顕在化するはずだった
  もの）も発見し、`HolidayType="会社休日"`の行だけを除外するよう修正した。
  `config/masters/business-calendar.csv`の8/10・8/13・8/14（会社休日）で再計算すると
  期待通り4日（8/7, 8/11, 8/12, 8/17）になる
  → [[2026-09-22 年休・慶弔・昼食の追加要望]] ／ `RequestForm.tsx` / `types.ts` / `requestTypes.ts`
- [x] ~~★特別休暇の事由カスケード（休暇区分→事由：慶弔休暇→内容①〜⑦）の新規設計~~ →
  **設計・実装済み（2026-09-22）**。就業規則46条/16条ベース。慶弔連絡（APP No.02）とは別物。
  `Reason`を`choicesFor`で休暇区分連動にし、新規列`SpecialLeaveDetail`（内容）を追加した
  → [[2026-09-22 年休・慶弔・昼食の追加要望]]
- [x] ~~遅刻/早退のとき終了日・日数を入力不可に、時刻入力を選択式にする~~ → **解消（2026-09-22）**。
  終了日は開始日に固定・日数は空欄のまま編集不可に。時刻は30分刻み・6:00〜22:00のプルダウンに変更
- [x] ~~承認者Person Pickerで氏名検索だと表示されない人がいる原因~~ → **部分解消（2026-09-22）**。
  メール検索が常に通る理由(検索を経由しないフリーテキスト経路)を特定し、氏名検索の結果上限を
  15→30に緩和した。根本原因(該当者側のEntra ID属性等)はテナント側の実機調査が必要なまま残る
- [x] ~~年休申請への代理申請機能の追加要望~~ → **解消（2026-09-22）**。慶弔連絡と同じ`InitiatorType`/
  `TargetEmployee`のパターンで実装
- [ ] 既存3系統（紙 / 購買前任者システム / PowerApps）へのアクセス権と、既存データの移行要否
- [ ] 年休カレンダーの要件（そもそもカレンダーなのかリスト表示なのか）
- [ ] 全社展開に伴うリスト名の変更（欠勤・遅刻・早退まで含む名称にするか）— 見送り中
- [x] ~~承認者の自動設定（上長マスタ）~~ → **2026-09-09、不採用で決着。履歴デフォルト表示を実装**
  → [[テーマ2 - 承認者をどう決めるか]]
- [ ] Power Automate フローの実装
